<% @leafs.each do |leaf| %>
	<div class="leaf">
		<div class="info clearfix">
			<span class="avatar left"><%= my_image_tag(leaf.provider.avatar,leaf.provider.weibo) %></span>
			<span class="name left">
				<%= link_to leaf.provider.user_name,"/p/#{leaf.provider.id}" %>
			</span>
			<span class="time right"><%= time_stamp(leaf.time_stamp) %></span>
		</div>
		<div class="content">
			<%= auto_link(leaf.content) %>
		</div>
		<% if leaf.image %>
			<div class="img"><%= image_tag(leaf.image(:medium)) %></div>
		<% end %>
		<% if !leaf.video.blank? %>
			<video preload="auto" controls height=250 width=300 src="<%= leaf.video %>" webkit-playsinline></video>
		<% end %>
	</div> 
<% end %>