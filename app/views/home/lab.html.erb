<div id="lab">
	<span class="big">媒体，传播渠道</span>
	<span class="big">
		教育资源／教师／课本
	</span>
	<span class="big">
		教育界的大众点评，汽车界的大众点评，电视节目界的大众点评，图书界的大众点评，服装界的大众点评
	</span>
	<span class="big">
		微博聚合，筛选推荐，分类
	</span>
	<span class="big">
		基于app push的单词记忆法
	</span>
	<div id="cav">
		<canvas id="canvas2" width="250" height="300" style="background-color:#fff;">   
		    你的浏览器不支持 Canvas 标签，请使用 Chrome 浏览器 或者 FireFox 浏览器   
		</canvas>
	</div>  

</div>
<script type="text/javascript" charset="utf-8">
	$(function(){
		
		function init(){
			var sun = new Image();
			var moon = new Image();
			var earth = new Image();
		  sun.src = '/assets/omg.png';
		  moon.src = '/assets/omg.png';
		  earth.src = '/assets/omg.png';
		  setInterval(draw,100);
		}

		function draw() {
		  var ctx = document.getElementById('canvas').getContext('2d');

		  ctx.globalCompositeOperation = 'destination-over';
		  ctx.clearRect(0,0,300,300); // clear canvas

		  ctx.fillStyle = 'rgba(0,0,0,0.4)';
		  ctx.strokeStyle = 'rgba(0,153,255,0.4)';
		  ctx.save();
		  ctx.translate(150,150);

		  // Earth
		  var time = new Date();
		  ctx.rotate( ((2*Math.PI)/60)*time.getSeconds() + ((2*Math.PI)/60000)*time.getMilliseconds() );
		  ctx.translate(105,0);
		  ctx.fillRect(0,-12,50,24); // Shadow
		  ctx.drawImage(earth,-12,-12);

		  // Moon
		  ctx.save();
		  ctx.rotate( ((2*Math.PI)/6)*time.getSeconds() + ((2*Math.PI)/6000)*time.getMilliseconds() );
		  ctx.translate(0,28.5);
		  ctx.drawImage(moon,-3.5,-3.5);
		  ctx.restore();

		  ctx.restore();

		  ctx.beginPath();
		  ctx.arc(150,150,105,0,Math.PI*2,false); // Earth orbit
		  ctx.stroke();

		  ctx.drawImage(sun,0,0,300,300);
		}
	})
</script>