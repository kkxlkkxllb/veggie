#top_nav.navbar.navbar-fixed-top
	.navbar-inner
		.container-fluid
			%button.btn.btn-navbar(type="button" data-toggle="collapse" data-target=".nav-collapse")
				- 3.times do
					%span.icon-bar
			%span.brand.dancer
				17up
				- if @leafs and params[:pid]
					%span.tag
						&raquo;
						= Provider.find(params[:pid]).user_name
			.container.nav-collapse
				%ul.nav
					%li{:class => ("active" if @leafs)}
						= link_to leafs_path do
							%i.icon-leaf
							= t("leafs.title")
					%li{:class => ("active" if @courses)}
						= link_to words_path do
							%i.icon-book
							= t("words.title")
					%li{:class => ("active" if @users)}
						= link_to hot_path do
							%i.icon-flag
							= t("square.title")
					%li{:class => ("active" if @quotes)}
						= link_to quote_path do
							%i.icon-comments-alt
							= t("quote.title")
				- if current_member
					%ul.nav.pull-right
						%li{:class => ("active" if action_name == "dashboard")}
							%a{:href => account_path}
								= current_member.name
						%li{:class => ("active" if @setting)}
							%a{:href => setting_path }
								%i.icon-wrench
						%li
							%a(href = "/members/sign_out" data-method="delete" rel="nofollow")
								%span.label.label-important
									%i.icon-off
									登出
		