#magic_images_modal.hide.modal.magic
	.modal-header
		%button.close(type="button" data-dismiss="modal")
			%span.icon-remove-circle
		%h3
			%i.icon-magic
			你可以使用关联身份取图
	.modal-body
		.errors.alert.alert-error
			%button(type="button" class="close" data-dismiss="alert")
				%span.icon-remove-circle
			TAT，你还没有这些技能呢，快去关联对应的帐户吧
			= link_to (setting_path + "#provider") do
				%span.icon-fighter-jet
		.magic_items
			- %w{instagram tumblr}.each do |p|
				%a{data: p}(href = "#provider")
					= image_tag("icon/#{p}_big.png",:title => p)
		.images
		
